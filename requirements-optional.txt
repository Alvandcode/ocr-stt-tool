# Optional extras -- install only what you need.
# Microphone input (needs system portaudio first):
#   Windows: pip install pipwin && pipwin install pyaudio
#   macOS:   brew install portaudio && pip install pyaudio
#   Linux:   sudo apt install portaudio19-dev && pip install pyaudio
PyAudio==0.2.14  # optional: microphone only, NOT needed for file transcription

# Convert MP3/OGG/M4A to WAV first (needs system ffmpeg):
#   sudo apt install ffmpeg / brew install ffmpeg / winget install ffmpeg
pydub==0.25.1  # optional: audio conversion helper

# OCR fallback for scanned PDFs (needs system poppler + tesseract):
#   sudo apt install poppler-utils tesseract-ocr tesseract-ocr-fas
pdf2image==1.17.0  # optional: scanned-PDF OCR fallback (--ocr-fallback)

# Offline speech-to-text (no Google API, no internet; ~1-3 GB model download
# on first run depending on --whisper-model):
faster-whisper==1.2.1  # optional: --engine whisper

# Better Persian normalization (otherwise a stdlib fallback is used):
hazm==0.10.0  # optional: improves --normalize-fa
